# Santigrey Enterprises -- Master Checklist

**Owner:** Paco (COO)
**Source of truth:** This file. All other docs feed it; it feeds none.
**Location:** Primary on CiscoKid `/home/jes/control-plane/CHECKLIST.md` + GitHub origin/main (canonical via git push). iCloud `/AI/Santigrey/CHECKLIST.md` is a CEO-convenience read-only copy, no longer Paco-managed (canon-flip 2026-04-26 Day 72).
**Last updated:** 2026-04-26 night (Day 72) -- B1-Garage Phase D confirmed (Garage operational: layout dc1/4T/256p, 3 buckets, root S3 key); Phase E GO (CiscoKid->Beast LAN smoke); B2b bit-identical across A/A2/B/C/D
**Update rule:** Paco updates after every closed task or every CEO direction change. Status legend: `[ ]` open, `[~]` in progress, `[x]` done, `[!]` blocked, `[-]` deferred.

---

## P0 -- URGENT (external deadline)

- [ ] **Capstone lane decision** for Per Scholas instructor meeting Monday 2026-04-27. Three lanes documented (Path A: Alexandra-derived RAG slice / Path B: rubric-suggested project / Path C: both as instructor options). CEO has all day Sunday + most of Monday before meeting. **Owner: CEO.**

## P1 -- ACTIVE WORK (this session)

- [x] D1 -- Lift four Pydantic input limits in `mcp_server.py` (PD shipped; Paco gate PASS). Code commit `3cb303c`, session commits `b43966e` + `1d9cbe8`.
- [x] **CAPACITY_v1.1** -- DRAFT shipped 2026-04-26 AM, RATIFIED 2026-04-26 PM (Day 72) by CEO. Three v1.0 errors corrected (phantom MCP migration removed, fictional iMessage/HomeKit bridges reclassified as future-conditional, Mac mini scope tightened). 133 lines.
- [x] **Mac mini scope lock** -- RATIFIED by CEO 2026-04-26. Principle locked: scope drives workflow, not reverse. Mac mini = Apple-bound infrastructure only + remains CEO daily-driver workstation (separate concern outside org chart). Footprint verified: Claude Desktop / Cowork host, AgentOS Refresh, Tailscale, OpenSSH. No iMessage/HomeKit bridges, no MCP termination.

## P2 -- DATA PLANE SEQUENCE (post-D1)

- [x] **D2** -- add `homelab_file_write` MCP tool. SHIPPED 2026-04-26 (code commit `faa0d6a`, session commit `8e602a7`). All 6 gate tests PASS: atomic write+read roundtrip (unicode + smart quotes + newlines), mkdir_parents on path with space, create-mode collision rejected, file_mode 0644 + 0600 verified, binary=True 32 bytes 0x00-0x1f byte-perfect (sha256 630dcd29...10dd canonical). PD `str_strip_whitespace` omission ratified (correctness over consistency).
- [ ] **D3** -- add `homelab_file_transfer` MCP tool (host-to-host). **Owner: Paco spec, PD execute.**
- [ ] **D4** -- streaming output support (move off `subprocess.run capture_output=True`). **Owner: Paco spec, PD execute.**
- [x] **A** -- discipline shift: rsync/HTTP/native replication for bulk; MCP for control only. RATIFIED by CEO 2026-04-26 as Rule 1 in `docs/STANDING_RULES.md` (3989 bytes, hash 141f04c087c78d2d8b1e02ffa8305cac, 68 lines). Standing rule now effective; all six plan-level decisions verified and approved as-is.
- [~] **B1** -- **Garage v2.1.0** on Beast (S3-compatible object store; **PIVOTED from MinIO** 2026-04-26 Day 72 after MinIO Community Edition archive 2026-02-14 surfaced during Phase B verification). RATIFIED 2026-04-26 Day 72 by CEO after 5-candidate exhaustive comparison. Spec at `tasks/B1_garage_beast.md` (29228 bytes / 6 phases A/A2/B/C/D/E/F / 8 acceptance gates). Single-node Docker Compose on Beast at `/home/jes/garage-beast/` with bind-mount data at `/home/jes/garage-data/{meta,data}`; LAN-bind 192.168.1.152:3900 (S3) + localhost-only 127.0.0.1:3903 (admin); 3 pre-created buckets (atlas-state, backups, artifacts) with single root S3 access key. Old MinIO spec at `tasks/B1_minio_beast.md` is DEPRECATED (kept for git history). MinIO Phase B in-flight rollback handled by Phase A2 of new spec. Net pivot cost ~40-60 min. 50-80 min PD estimate for new run. Queued for PD via Cowork. **Owner: Paco spec, PD execute.**
- [x] **B2a** -- install PostgreSQL on Beast (Docker pgvector/pgvector:pg16, greenfield). SHIPPED 2026-04-26 by PD (ship report at `/home/jes/postgres-beast/B2a_ship_report.md` on Beast, md5 b9d862fcebbe78317a0081d052121294). Paco independent gate PASS 7/7 from fresh shell. PG 16.13 + pgvector 0.8.2 healthy on 127.0.0.1:5432. Two Paco-authorized deviations documented (Compose v5.1.3 plugin bootstrap; health-poll replacing sleep 15). **Owner: Paco spec, PD execute. CLOSED.**
- [x] **B2b** -- **CLOSED 2026-04-26 Day 72.** Logical replication CiscoKid -> Beast LIVE; 12/12 acceptance gates PASS via Paco independent verification gate from fresh shell. 5,795 rows replicated across 13 tables byte-perfect; slot streaming async with zero lag; live INSERT+DELETE smoke verified within 10s each. 9 phases (A-I) over ~3h; 4 Paco-authorized deviations; 6 P6 lessons banked. Ship report at `postgres/B2b_ship_report.md`. Close-out at `docs/paco_response_b2b_independent_gate_pass_close.md`. **P3 Atlas-on-Beast charter implementation unblocked.** See audit trail #102.
- [-] **C** -- cable up idle high-speed ports. Defer until 1 GbE actually saturates.
- [-] **E** -- replace MCP-over-SSH with MCP-over-HTTP-API. Defer one quarter.

## P3 -- HARDWARE ORG REDISTRIBUTION (CAPACITY_v1.1 ratified; redistribution unblocked)

- [ ] **SlimJim** -- add Prometheus + Grafana, deploy node_exporter on every node. **Owner: Paco spec, PD execute.**
- [ ] **Beast** -- becomes Atlas's home (charter revision from CAPACITY_v1.0). Build deferred until B1+B2 land. **Owner: Paco spec, PD execute.**
- [-] **SlimJim** -- Wazuh/Suricata security monitoring. Deferred (needs careful tuning).
- [ ] **Pi3** -- assign role. Currently registered (192.168.1.139, sloanzj) but unprobed and unassigned. **Owner: CEO direction needed.**
- [ ] **JesAir** -- evaluate beyond thin client (Apple Silicon, capable of more). **Owner: CEO direction needed.**
- [ ] **Cortez** -- evaluate beyond thin client (NPU-equipped per CEO flag). **Owner: CEO direction needed.**
- [ ] **Cortez** -- Tailscale resilience hardening (Day 69 carryover; auto-restart + boot-start + DERP keepalive). **Owner: PD execute when Cortez online.**

## P4 -- ORG CHARTERS

- [x] **CHARTERS_v0.1.md ratification** -- six role charters + Alexandra platform charter. RATIFIED 2026-04-26 Day 72 by CEO. Atlas-on-Beast revision applied at ratification per CAPACITY_v1.1 (Atlas builds on Beast for Postgres replica + MinIO + embeddings + tool execution; inference offloaded to Goliath Qwen 2.5 72B over LAN). Pulled from iCloud to CiscoKid as part of iCloud canon-flip; now canonical at `/home/jes/control-plane/CHARTERS_v0.1.md` on CiscoKid + GitHub. 11074 bytes, 7 charters + Platform charter + Ratification audit section.
- [ ] **PD title communication** -- announce Head of Engineering scope to PD. Deferred until charter ratification. **Owner: CEO + Paco.**
- [ ] **Family Office charter** -- explicitly drafted or explicitly kept informal. **Owner: CEO direction.**
- [ ] **Sub-agent definitions** inside each department (Mr Robot, Mercury, future agents). **Owner: Paco draft.**
- [ ] **Inter-department SOPs** -- how Engineering hands work to Operations, etc. **Owner: Paco draft.**
- [ ] **Brand & Market quarterly plan** -- separate strategy document. **Owner: CEO + Paco.**

## P5 -- DAY 69 CARRYOVERS (still pending)

- [ ] phase-4-sanitizer rebase + step 6/12 resume. **Owner: PD.**
- [ ] Methodology doc "verify what depends on a credential." **Owner: Paco draft.**
- [ ] Credentials inventory doc. **Owner: Paco draft.**
- [ ] Calendar reminder 2026-05-24 -- drop `_retired_` tables. **Owner: CEO add to calendar.**
- [ ] Cortez `cortez_AI_Operator` PAT (regenerated, unused) -- delete on GitHub at convenience. **Owner: CEO.**

## P6 -- METHODOLOGY (Paco self-improvement)

- [ ] Codify into anchor: spec template for live-service tasks must answer (1) "is restart safe right now?", (2) "what does the health check actually mean?", AND (3) "where does the PD-to-Paco report land and how is it shared (file path, naming convention, transmission mechanism)?" -- D1 surfaced (1)+(2); D2 surfaced (3) (CEO had to prompt PD for the report after ship). **Owner: Paco.**
- [ ] Codify rule: **no claim about state without source verification first.** Already in anchor; needs reinforcement after Day 70 + Day 72 AM corrections. **Owner: Paco.**
- [ ] Codify into spec template: deferred-restart pattern MUST bundle verification into the same nohup subshell, writing to `/tmp/<task>_verify.out`. PD surfaced this from D2 -- avoids the cgroup-death race that otherwise requires fishing verification out of a follow-up MCP call (which itself fails because the channel just bounced). **Owner: Paco.**

---

## Closed (audit trail)

- [x] **2026-04-25 Day 70 AM** -- Org chart locked v0.4 (3 departments + 2 CEO-direct + Alexandra platform).
- [x] **2026-04-25 Day 70 AM** -- CHARTERS_v0.1.md drafted (164 lines, 7 charters).
- [x] **2026-04-25 Day 70 PM** -- Hardware audit completed across all reachable nodes.
- [x] **2026-04-25 Day 70 PM** -- MCP fabric diagnosed (control plane only, no data plane). Five-path fix sequence chosen: D -> A -> B -> C(maybe) -> E(deferred).
- [x] **2026-04-25 Day 70 PM** -- CAPACITY_v1.0.md drafted (104 lines, DRAFT). Superseded by v1.1 once corrections apply.
- [x] **2026-04-25 Day 70 PM** -- D1 task spec drafted, CEO-approved, queued for PD.
- [x] **2026-04-26 Day 71** -- D1 SHIPPED by PD (commit `3cb303c`). Paco verification gate PASS.
- [x] **2026-04-26 Day 72 AM** -- Mac mini footprint verified. MCP migration confirmed phantom (already on CiscoKid). Three v1.0 errors identified for v1.1 correction.
- [x] **2026-04-26 Day 72** -- CAPACITY_v1.1.md drafted (133 lines, hash 1de8a2cc0f01cd7b32e3170bf2ab4e82). Three v1.0 errors corrected. Saved to iCloud + mirrored to CiscoKid. v1.0 retained as superseded backup.
- [x] **2026-04-26 Day 72** -- Mac mini scope ratified. Principle locked: scope drives workflow, not reverse.
- [x] **2026-04-26 Day 72** -- D2 spec drafted (213 lines, hash 137cabe0c04165817029e0d6a49e96a1). CEO-approved D-A-B-C-E sequence within data plane: D2 first (highest leverage), then A discipline rule, then B2 (Atlas hard prereq), then D3, B1, D4, then Atlas. Spec queued for PD. Saved to iCloud + CiscoKid as DRAFT.
- [x] **2026-04-26 Day 72** -- D2 SHIPPED by PD (code commit `faa0d6a`, session commit `8e602a7`). Paco gate PASS (all 6 tests). PD methodology contributions: (a) bundled-verification deferred-restart pattern, now standard; (b) `str_strip_whitespace` omission ratified for content fidelity. Three methodology lessons captured into P6.
- [x] **2026-04-26 Day 72** -- A drafted as Rule 1 in new canon doc `docs/STANDING_RULES.md` (3989 bytes, 68 lines, hash 141f04c087c78d2d8b1e02ffa8305cac). MCP-fabric-is-control-not-bulk principle codified with explicit in/out scope, transport routing rules, and violation handling. Awaiting CEO ratification.
- [x] **2026-04-26 Day 72** -- A RATIFIED by CEO as-is (no rework). All six plan-level decisions verified retroactively (3 already-flagged + 3 surfaced for measure: new doc choice, storage location pattern, 1MB tool-output threshold). Rule 1 (MCP fabric is for control, not bulk data) effective immediately. P2 data plane advances to B2 next.
- [x] **2026-04-26 Day 72** -- B2 shallow probe complete (commit `a23caf1`, 113-line investigation report). Q3 resolved -> split into B2a + B2b. Q4-Q7 ratified by CEO with Paco picks: Q4=C (public+agent_os, exclude mercury), Q5=A (Docker pgvector/pgvector:pg16), Q6=A (LAN rebind + layered ACLs), Q7=C (bundle WAL change into B2b). B2a spec drafted (6186 bytes, greenfield Postgres on Beast, no CiscoKid dependency at install time). Awaiting CEO ratification.
- [x] **2026-04-26 Day 72** -- B2a spec RATIFIED by CEO as-is (no rework). Cowork prompt provided; queued for PD execution. Awaiting ship report at `/home/jes/postgres-beast/B2a_ship_report.md` on Beast.
- [x] **2026-04-26 Day 72** -- B2a SHIPPED by PD (ship report md5 `b9d862fcebbe78317a0081d052121294`, 220 lines, on Beast at `/home/jes/postgres-beast/B2a_ship_report.md`). All 7 acceptance gates PASS. Paco independent verification gate from fresh shell: 7/7 PASS, file integrity preserved (compose.yaml `a9a7404d...`, init SQL `899c6932...`), container ID `61acfaf455d8...` running image SHA `8ed3192326bb...`. Two Paco-authorized deviations documented in ship report: (1) Compose v5.1.3 plugin bootstrap (Beast docker.io lacks v2 plugin; sha256 verified against Docker upstream); (2) Spec Step 6 `sleep 15` replaced with health-poll loop (P6 lesson: container-up steps should poll until healthy with explicit timeout cap, not sleep for guessed duration). PG 16.13 + pgvector 0.8.2 running on Beast at 127.0.0.1:5432, controlplane DB owned by admin (Superuser+Replication=t), pgdata 48.21MB. Three P6 methodology lessons captured. B2a CLOSED. B2b (logical replication CiscoKid->Beast) drafting unblocks; Atlas-on-Beast (P3) hard prereq satisfied. Task pipeline `6d5102a5-93e8-4972-846f-9acbabe2b795` ready for CEO mark-done. Doc chain: 10 B2a docs total (2 paco_request, 4 paco_review, 4 paco_response in `/docs/`; ship report on Beast).
- [x] **2026-04-26 Day 72** -- CAPACITY_v1.1 RATIFIED by CEO as-is. Hardware org chart canon-locked. P3 hardware redistribution unblocks (SlimJim Prometheus+Grafana, Beast=Atlas home, Pi3/JesAir/Cortez role evaluations).
- [x] **2026-04-26 Day 72** -- iCloud canon-flip RATIFIED by CEO. Standing convention now: CiscoKid + GitHub origin/main are canonical for all Paco-managed artifacts (CHECKLIST, STANDING_RULES, CAPACITY, CHARTERS, /tasks specs, /docs correspondence). iCloud `/AI/Santigrey/` becomes a CEO-convenience read-only copy not maintained by Paco. Macmini SSH pipe is no longer treated as data-sync infrastructure (Rule 1 corollary). Stale iCloud copies will be allowed to drift; CEO can manually pull from GitHub when desired. CHECKLIST + STANDING_RULES Location/Mirror lines updated. Memory edit pending.
- [x] **2026-04-26 Day 72** -- CHARTERS_v0.1 RATIFIED by CEO with Atlas-on-Beast revision applied (per CAPACITY_v1.1 ratification: Atlas home = Beast, inference = Goliath). Pulled from iCloud one final time as part of canon-flip transition; now canonical on CiscoKid + GitHub. Build status revision: Atlas to be built on Beast (Postgres replica + MinIO + embeddings + tool execution), inference offloaded to Goliath (Qwen 2.5 72B over LAN), depends on B2b + B1 landing first. Six P4 deferred items remain (Family Office charter, sub-agent definitions, inter-dept SOPs, Atlas build spec, shared-state architecture, Brand & Market quarterly plan).
- [x] **2026-04-26 Day 72** -- B2b spec drafted (19341 bytes / ~440 lines, hash via md5sum). Logical replication CiscoKid (publisher PG 16.11) -> Beast (subscriber PG 16.13) for `public` + `agent_os` schemas; `mercury` deliberately excluded per Q4=C. Q1-Q7 picks integrated into spec. Four NEW structural picks flagged: (1) pg_hba.conf bind-mount approach Option B preferred (config-as-code, mirrors B2a init/), (2) Service ordering Option A preferred (accept ~5-30s downtime, dependents auto-reconnect per Day 48 hardening), (3) Subscription password Option A preferred (admin/adminpass; replicator-role separation deferred to P5 credentials inventory), (4) Schema bootstrap transport Option A preferred (pg_dump --schema-only + scp + psql; standard idiom; Rule 1 compliant). 12-item acceptance gate, full rollback procedure, downtime safety analysis. Awaiting CEO ratification.
- [x] **2026-04-26 Day 72** -- B2b RATIFIED by CEO (Option 1, all four picks as Paco recommended): (1) pg_hba.conf via bind-mount (Option B, config-as-code), (2) accept ~5-30s CiscoKid Postgres downtime during Phase E (Option A; dependents auto-reconnect per Day 48), (3) subscription password admin/adminpass matching publisher (Option A; replicator-role separation deferred to P5), (4) schema bootstrap via pg_dump --schema-only + scp + psql (Option A; standard idiom, Rule 1 compliant). Spec status DRAFT -> RATIFIED. Queued for PD via Cowork prompt with `docs/paco_response_b2b_ratification_phase_a_go.md` as routing handle. PD begins Phase A (CiscoKid pre-change capture + conf/ directory creation + backup compose.yaml + pg_hba.conf.original capture).
- [x] **2026-04-26 Day 72** -- B2b Phase A CONFIRMED by Paco. PD captured rollback artifacts on CiscoKid: /tmp/compose.yaml.b2b-pre-backup (md5 b7bbe49cd6e113a450eba8f72bcdb61a, 506 bytes) + /tmp/pg_hba.conf.original (md5 3f1a04ebe46ac5af105962d6be6360c2, 5743 bytes). conf/ dir created empty at /home/jes/control-plane/postgres/conf/. On-disk compose.yaml unchanged (matches backup). Pre-change PG state matches preflight: wal_level=replica, single 127.0.0.1:5432 listener, 0 publications, 0 slots, PG 16.11. Independent verification by Paco from fresh shell: ALL ARTIFACTS MATCH PD HASHES. Phase B GO authorized at docs/paco_response_b2b_phase_a_confirm_phase_b_go.md WITH AMENDMENT: pg_hba.conf Beast entries use scram-sha-256 instead of spec-literal md5. Reason: admin role password stored as SCRAM-SHA-256 (verified pg_authid.rolpassword starts with `SCRAM-SHA-256$`); md5 auth method incompatible; Phase H would have failed. Spec text at tasks/B2b_logical_replication.md Phase B example amended this commit (md5 -> scram-sha-256, 2 lines). P6 lesson captured: For PG-touching specs, verify password_encryption setting and choose pg_hba.conf auth method accordingly. Default to scram-sha-256 unless infrastructure constrains.
- [x] **2026-04-26 Day 72** -- B2b Phase B CONFIRMED by Paco. PD wrote `/home/jes/control-plane/postgres/conf/pg_hba.conf` (md5 2138efc3a90ab513cf5aa1fff1af613e, 1124 bytes, 15 lines, 9 data) via heredoc. Beast entries on lines 12+13 both `scram-sha-256` (per amendment); catch-all preserved on line 15. Container untouched: still 127.0.0.1:5432->5432/tcp, wal_level=replica, internal pg_hba.conf md5 unchanged (3f1a04ebe46ac5af105962d6be6360c2). PD self-corrected Phase A note re: replication-pseudo-DB rule (good engineering hygiene). Independent verification by Paco: ALL ARTIFACTS MATCH. Phase C+D GO authorized at docs/paco_response_b2b_phase_b_confirm_phase_c_d_go.md WITH TWO CORRECTIONS surfaced from independent verification: (1) UFW rule must use `ufw insert 18` not `ufw allow`, because pre-existing DENY rules at positions 18 (IPv4) + 25 (IPv6) would otherwise win the rule evaluation; (2) Disclosure that Docker iptables (PREROUTING DNAT + DOCKER-USER chain) bypasses UFW INPUT-chain filter rules, so the UFW rule is documented defense-in-depth. Actual security gates: LAN-only listener (192.168.1.10:5432, not 0.0.0.0) + pg_hba.conf scram-sha-256 + admin SCRAM hash. P5 carryover added: DOCKER-USER chain hardening for B2b LAN-published Postgres. P6 carryover added: capture pre-existing UFW rule layout before drafting any Phase D directive; default to `ufw insert <N>` when ordering matters. Phase E (the recreate) gated behind separate paco_response GO -- NOT bundled into C+D.
- [x] **2026-04-26 Day 72** -- B2b Phase C+D CONFIRMED by Paco. PD wrote new compose.yaml (md5 ffbfbfa8350bf92bb4d54db490e90221, 699 bytes/27 lines); diff vs `/tmp/compose.yaml.b2b-pre-backup` shows exactly 3 functional changes (port 127.0.0.1->192.168.1.10, bind mount conf/pg_hba.conf:ro, command stanza for hba_file + wal_level=logical) + 1 cosmetic blank-line drop. `docker compose config` validates clean (exit 0, bind source path correct, host_ip 192.168.1.10, command resolved correctly). UFW: `ufw insert 18 allow from 192.168.1.152 to any port 5432 proto tcp` succeeded; allow now at [18], DENY pushed [18]->[19] (IPv4) and [25]->[26] (IPv6); 29 rules total. Container UNTOUCHED on all 4 invariants: still 127.0.0.1:5432->5432/tcp, still wal_level=replica, internal pg_hba.conf still 3f1a04ebe46ac5af105962d6be6360c2 (Phase A original), PID 1 args still bare `postgres`. Independent verification by Paco: ALL ARTIFACTS MATCH. Phase E GO authorized at docs/paco_response_b2b_phase_c_d_confirm_phase_e_go.md with deferred-subshell verification pattern (from D2 memory feedback Day 71): wraps recreate + 60s health-poll + 9-item gate verification in nohup-disowned subshell writing to /tmp/B2b_phase_e_verify.log; SSH session returns immediately; subsequent calls poll-for-COMPLETE-marker then cat the log; insulates verification record from any session-level disruption during the ~5-30s downtime window. Phase E is the FIRST service-affecting phase of B2b -- orchestrator/dashboard/Telegram bot will see brief connection errors during recreate, auto-reconnect per Day 48 hardening. 9-item Phase E acceptance gate. Rollback procedure live if any gate fails (cp backup + recreate + ufw delete).
- [x] **2026-04-26 Day 72** -- B2b Phase E CONFIRMED by Paco. PD ran the deferred-subshell pattern verbatim from auth doc; subshell PID 2875350 wrote /tmp/B2b_phase_e_verify.log. Recreate clean: docker compose down (1s) + up -d (0s return) + 4 health-polls before healthy. Measured downtime **16s** (down-start 22:23:28 to first healthy 22:23:44), within 5-30s envelope. All 9 acceptance gates PASS via independent fresh-shell cross-check by Paco: container Up healthy on 192.168.1.10:5432 (NOT 127.0.0.1, NOT 0.0.0.0, count=1), wal_level=logical, hba_file=/etc/postgresql/pg_hba.conf, internal pg_hba.conf md5=2138efc3a90ab513cf5aa1fff1af613e (matches Phase B byte-for-byte), PID 1 args=`postgres -c hba_file=/etc/postgresql/pg_hba.conf -c wal_level=logical`, max_wal_senders=10, max_replication_slots=10, pg_isready accepting, RestartCount=0, pg_publication=0 + pg_replication_slots=0 (clean baseline for F/G/H). Rollback artifacts intact. **Observation correction:** PD reported `systemctl is-active ai-operator = inactive` and inferred orchestrator was down; this was wrong-service-name read. Actual unit is `orchestrator.service` (NOT ai-operator); orchestrator IS running (PID 2688, uvicorn app:app on :8000). Auto-reconnect resilience IS exercised normally. **P6 lesson captured:** project-codename != systemd-unit-name; verify via systemctl list-units before is-active. **Image SHA observation logged for ship report:** CiscoKid pgvector/pgvector:pg16 PG 16.11 (SHA 0a07c411...) vs Beast PG 16.13 (SHA 8ed31923...); subscriber-newer is canonical safe direction for logical replication. **MCP wedge mid-commit:** homelab-mcp.service became unresponsive for ~30 minutes during Sloan off-network window between writing the Phase F auth doc and landing the audit/commit; recovered on its own; no state loss. Phase F GO authorized at docs/paco_response_b2b_phase_e_confirm_phase_f_go.md (15313 bytes / 251 lines, md5 3ca5843e8750690ad632ef84517fb717): 7-step pg_dump + filter-vector-extension + SCP + psql + verify directive, 9-item Phase F gate, full rollback. Pick 4 ratified Rule 1 SCP carve-out applies (bulk SQL via SCP, NOT MCP). Vector-extension filter (sed) explicitly authorized since Beast already has vector from B2a init.
- [x] **2026-04-26 Day 72** -- **CANON BACKLOG CATCH-UP** committed in commit d7f61fd. Significant hygiene drift had accumulated: the paco_review/paco_response/paco_request docs from B2a (8 docs) and B2b Phases A through E (4 reviews + 1 response) were on disk but untracked in git. Phase B `postgres/conf/pg_hba.conf` (md5 2138efc3...) and Phase C `postgres/compose.yaml` (md5 ffbfbfa8...) were also LIVE on disk and powering the running container, but never committed. D2 spec at `tasks/D2_add_file_write_tool.md` (from Day 71) and one ancillary `paco_request_r640_fan_control_idrac9_7x.md` were similarly untracked. All 19 files bundled into commit d7f61fd (2509 insertions) so origin/main reflects current operational reality. **P6 lesson captured:** post-merge hygiene check after each ship -- run `git status` to verify no canonical artifacts (specs, infra files, /docs correspondence) are sitting untracked. Ephemeral backup `mcp_server.py.pre-pi3-20260425-012451` moved to /tmp (not canon). Sed-multi-line-with-leading-dash error during the d7f61fd CHECKLIST audit-entry insert caused these two audit entries to be missed; landed via sed `r` from tempfile in the follow-up commit instead. **P6 lesson captured:** for multi-line CHECKLIST audit inserts where lines start with `-`, use `sed r` from a tempfile rather than inline `a` with embedded newlines.
- [x] **2026-04-26 Day 72** -- B2b Phase F FAILED on first attempt at gate 6: psql ON_ERROR_STOP halted on `ERROR: schema "public" already exists`. Root cause: pg_dump --schema=public emits a literal `CREATE SCHEMA public;` line that collides with PG initdb's pre-created public schema on every fresh DB. Beast post-failure state: orphan empty agent_os schema + public+vector intact (NOT yet rolled back per PD's prudent paco_request workflow). PD surfaced three issues via paco_request_b2b_phase_f_failure.md (12373 bytes): (1) the failure root cause as above, (2) **subtle issue with spec literal rollback**: B2a's init script created vector unqualified, landing in public schema on Beast; spec's literal `DROP SCHEMA public CASCADE` would CASCADE-drop vector and degrade Beast below B2a baseline (verified: pg_extension shows vector|public namespace). PD flagged BEFORE executing rollback -- exactly correct paco_request usage. (3) **SSH-piped command exit-code masking**: `cat | docker exec | tail` chain returned tail's success code, hiding psql's actual ERROR exit. PD recommended Option B (targeted rollback + sed filter retry). **Option B AUTHORIZED** at docs/paco_response_b2b_phase_f_failure_recovery_option_b.md (13056 bytes). Authorized changes: (a) targeted rollback `DROP SCHEMA agent_os CASCADE` only -- preserves public + vector; (b) extended sed filter adds `^CREATE SCHEMA public;$` line removal alongside vector-extension filtering; (c) pipefail / PIPESTATUS pattern for psql-via-SSH exit-code capture. Three P6 lessons captured: (P6-A) `pg_dump --schema=public` emits literal CREATE SCHEMA public collision -- spec templates for logical-replication bootstrap need to anticipate this; default to sed filter or pg_restore --clean; (P6-B) SSH-piped commands need `set -o pipefail` or PIPESTATUS to surface real exit codes; future spec patterns should never run psql-via-SSH through tail/head/etc without exit-code capture; (P6-C) vector extension cascade on rollback -- B2a's unqualified `CREATE EXTENSION vector` lands in public; rollback specs touching public must either drop vector first explicitly OR use targeted DROP. Option C (pg_restore --clean) noted as alternative pattern for future spec-template work. PD waits for explicit Cowork relay; will execute Steps 1-6 of recovery directive and write `paco_review_b2b_phase_f_retry_success.md` (or `paco_request_b2b_phase_f_failure_v2.md` on further failure).
- [x] **2026-04-26 Day 72** -- B2b Phase F RETRY SUCCESS (Option B). PD executed all three Paco-authorized corrections verbatim: (1) targeted rollback `DROP SCHEMA agent_os CASCADE` only -- Beast restored to B2a-clean state with vector preserved at `public|vector|0.8.2` (verified post-rollback); (2) extended sed filter (3 patterns: vector ext + comment + public-schema line) -- diff vs v1 showed exactly 1 line removed at L32 (`CREATE SCHEMA public;`); (3) pipefail exit-code capture (`set -o pipefail` inside SSH-quoted string + tee) -- ssh-exit=0 reflects psql's actual success, 0 ERROR lines in /tmp/B2b_phase_f_retry_load.log. **All 10 gates PASS** via PD scorecard + independent Paco cross-check from fresh shell: Beast \dn=public+agent_os (mercury absent), public table count=12 (matches CiscoKid 12), agent_os count=1 (matches CiscoKid 1), all 13 table names byte-match between hosts, total Beast row count=0 (schema-only correctly), vector ext still `public|vector|0.8.2` (B2a invariant preserved end-to-end), Beast pg_subscription=0 + CiscoKid pg_publication=0 + pg_replication_slots=0 (clean for Phase G/H), filtered-v2 md5 16313cb5a00200049169e989d8468b34 parity confirmed both hosts. CiscoKid mercury count=4 correctly NOT replicated (Q4=C ratification honored). Schema files preserved on both hosts through Phase G/H per spec. PD's Gate 2 reframe accepted: source dump satisfies the gate, the v2 filter intentionally drops public-schema line because public pre-exists; future spec drafts will use formulation "source dump >= 2 CREATE SCHEMA AND filtered file's removed-lines == [authorized exclusions]" -- captured as P6 carryover. **Phase G GO** authorized at docs/paco_response_b2b_phase_f_retry_confirm_phase_g_go.md (10123 bytes): single SQL `CREATE PUBLICATION controlplane_pub FOR TABLES IN SCHEMA public, agent_os` on CiscoKid, 5-item acceptance gate (publication created with all 4 DML flags true + puballtables=false, 13 tables in pg_publication_tables, schema-scoped binding via pg_publication_namespace, CiscoKid still healthy with 0 replication slots since slots are subscriber-driven at Phase H). Phase G is fully reversible via single `DROP PUBLICATION controlplane_pub;` -- no service restart, no deferred-subshell pattern needed. Three P6 lessons from the failure-and-recovery cycle now have empirical validation: (P6-A) sed-filter pattern works for pg_dump CREATE SCHEMA public collision; (P6-B) pipefail-aware SSH command surfaces real exit codes; (P6-C) targeted rollback preserves B2a vector invariant.
- [x] **2026-04-26 Day 72** -- B2b Phase G CONFIRMED by Paco. PD ran `CREATE PUBLICATION controlplane_pub FOR TABLES IN SCHEMA public, agent_os` on CiscoKid; pure metadata operation, no service interruption. **All 5 gates PASS** via PD scorecard + independent Paco cross-check from fresh shell: pg_publication shows 1 row (`controlplane_pub | f | t | t | t | t` -- puballtables=false, all 4 DML flags true), pg_publication_tables shows 13 entries (12 public + 1 agent_os, mercury count=0, names byte-match Beast's loaded schema), pg_publication_namespace shows 2 rows (public + agent_os bound to controlplane_pub -- confirms schema-scoped binding which auto-tracks future tables), CiscoKid Up About an hour healthy on 192.168.1.10:5432 with RestartCount=0, pg_replication_slots=0 (subscriber-driven slot creation deferred to Phase H), pg_subscription=0 (publisher-only). PD's note about pg_publication_rel=0 explained: schema-scoped publications use pg_publication_namespace for dynamic membership rather than materializing in pg_publication_rel; `pg_publication_tables` view joins both correctly. Bonus discovery from independent verification: agent_tasks schema has `Publications: controlplane_pub` line in psql \d output -- confirms table-publication binding from the catalog side. agent_tasks selected for Phase H smoke test (id uuid PK with gen_random_uuid() default; created_by/assigned_to/title required NOT NULL; status defaults to 'pending_approval'). **Phase H GO** authorized at docs/paco_response_b2b_phase_g_confirm_phase_h_go.md (16361 bytes): deferred-subshell pattern (Phase E precedent) wraps CREATE SUBSCRIPTION + sync poll + slot verification + stat_replication check + smoke test (INSERT/verify/DELETE on agent_tasks with 10s replication wait + cleanup verification). Heredoc-via-stdin pattern explicitly authorized for the SQL (avoids nested-quote escaping nightmare for the connection string with single-quoted password). 11-item Phase H acceptance gate. Failure-mode triage included for auth/connection/sync-stuck scenarios. Phase H is the LAST service-affecting phase before Phase I cleanup + 12-gate ship report. Restart-safety expectation: zero downtime on either side; replication starts and continues. Pick 3 (admin/adminpass shared password) honored; replicator-role separation deferred to P5. Phase H is also where the LAN bind (Phase E) + UFW rule (Phase D) + pg_hba.conf entries (Phase B) all get exercised end-to-end for the first time -- if Phase B's scram-sha-256 amendment was correct, this is where it shows.
- [x] **2026-04-26 Day 72** -- B2b Phase H executed; replication INFRASTRUCTURE HEALTHY but verifier-script bugs left gate 7 (DELETE replication) UNTESTED + gate 10 (no smoke residue) FAILED. PD ran the deferred-subshell from auth doc; CREATE SUBSCRIPTION ssh-exit=0 in 1s, all 13 tables reached `srsubstate=r` in 15s (gate 11 PASS, sync time within envelope), slot active=t with `confirmed_flush_lsn=0/F2253F8` matching `pg_current_wal_lsn` (zero lag), pg_stat_replication shows pid=4271 application_name=controlplane_sub client_addr=192.168.1.152 state=streaming sync_state=async, Beast pg_subscription enabled with subpublications={controlplane_pub}, both containers healthy with RestartCount=0 each. **Smoke INSERT replicated cleanly**: post-hoc clean read-only query confirmed byte-identical row on both sides (id=4f6f22c0-78b6-41f4-ac0c-5d80f1a792ad, byte-match across created_by/assigned_to/title/created_at). **Two script-side bugs surfaced**: (Bug A, cosmetic) `srsubstate || '=>'` poll-display query failed PG 16 type strictness because `srsubstate` is `char(1)` not text; the separate `count <> 'r'` branch worked correctly so gate 3 detection was unaffected. (Bug B, smoke-test-killer) `psql -tAc "INSERT ... RETURNING id;"` capture of TEST_ID was polluted with command-tag output `\nINSERT 0 1` because `-tA` does NOT suppress command tags; subsequent verify and DELETE commands hit `invalid input syntax for type uuid` -- smoke DELETE never executed, leaving smoke residue row on both sides (gate 10 nominal FAIL but the residue itself is empirical proof replication works). PD prudently filed paco_request_b2b_phase_h_failure.md (11666 bytes) rather than improvising a manual DELETE -- second time this session that "Spec or no action" caught the right edge correctly. Gate scorecard at request time: 8 PASS / 1 UNTESTED (gate 7) / 1 PARTIAL (gate 8: ERRORs are script bugs not replication errors) / 1 FAIL (gate 10: residue exists). PD recommended Option A (single SQL DELETE + 10s wait + cross-check both sides). **Option A AUTHORIZED** at docs/paco_response_b2b_phase_h_failure_recovery_option_a.md (12670 bytes). Authorized: clean SQL DELETE on CiscoKid via LIKE pattern (no shell variable to pollute), 10s fixed wait (replication caught up), count cross-check on both sides; deferred-subshell explicitly waived as inappropriate for this bounded non-service-affecting operation. Three NEW P6 lessons banked: (#4) `srsubstate` is `char(1)` in PG 16+, cast to text for any concat in subscription-poll display queries; (#5) `psql -tAc` does NOT suppress command tags -- use `-tAqc` for clean shell-variable capture or pipe through `head -1` or regex-filter the output (default for spec templates: `-tAq`); (#6) log-grep gate-text precision -- gates that say "no ERROR lines in Beast subscriber logs" should reference structured fields like `pg_stat_subscription.last_error` rather than generic `docker logs | grep -i error` patterns. P6 count now at 6 across this session (3 from Phase F failure recovery + 3 from Phase H verifier-script bugs). PD's next deliverable consolidates original subshell evidence + post-hoc cross-check + Option A cleanup-verify into single `paco_review_b2b_phase_h_subscription.md` for 11/11 PASS sign-off. Phase I (cleanup + 12-gate ship report) gated behind separate Paco GO after that consolidated review.
- [x] **2026-04-26 Day 72** -- B2b Phase H CONFIRMED by Paco (consolidated review). PD's `paco_review_b2b_phase_h_subscription.md` (13055 bytes) consolidated three evidence streams into one 11/11 PASS scorecard: (1) original subshell evidence (CREATE SUBSCRIPTION ssh-exit=0 in 1s, sync poll all-r in 15s, slot active streaming, gates 1+2+3+4+5+11 PASS), (2) post-hoc cross-check (byte-identical smoke INSERT row replicated to Beast id=4f6f22c0-78b6-41f4-ac0c-5d80f1a792ad, gates 6+9 PASS, gate 8 PARTIAL with documented attribution), (3) Option A cleanup-and-verify (DELETE 1 on CiscoKid, both sides count=0 within 10s sleep, slot LSN advanced 0/F229038 -> 0/F22A9D0 with confirmed_flush 0/F229070 -> 0/F22AA08 returning to 0-byte lag, gates 7+10 PASS). Independent Paco cross-check from fresh shell confirmed all 11 gates: smoke residue 0/0 both sides, slot active=t with `confirmed_flush_lsn=0/F22AA08` matching `pg_current_wal_lsn` (zero lag), pg_stat_replication shows pid=4271 streaming async from 192.168.1.152, Beast subscription enabled with subpublications={controlplane_pub}, all 13 tables in srsubstate=r, both containers running with RestartCount=0 each, B2a vector invariant `public|vector|0.8.2` preserved end-to-end through B2b. **Beast ERROR enumeration verified exact:** total 7 ERROR lines in `docker logs control-postgres-beast`, distribution 3x Bug A (`char || unknown` type strictness from sync-poll display query) + 3x Bug B (`invalid input syntax for type uuid` from TEST_ID variable pollution) + 1x Phase F first-attempt initial schema collision (`schema "public" already exists`). Zero replication-worker errors. Gate 8 PASSES with full ERROR-line attribution. **Logical replication CiscoKid -> Beast is LIVE and verified bidirectional-aware** (INSERTs replicate via streaming WAL path; DELETEs replicate within 10s; slot tracks LSN progression with zero lag). Two new P6 lessons formally accepted: (#4) PG 16 char(1) strictness on `||` concat -- `srsubstate::text` cast required for any subscription-poll display query in PG 16+; (#5) psql `-tA` does NOT suppress command tags -- `$(psql -tAc "INSERT ... RETURNING id;")` captures `<uuid>\nINSERT 0 1` polluting subsequent UUID-typed shell variable uses; default for spec templates is now `psql -tAq` (with -q quiet flag) or pipe through head -1 or regex-filter. P6 count this session: 6 (3 from Phase F + 3 from Phase H verifier-script-bug recovery). **Phase I GO** authorized at docs/paco_response_b2b_phase_h_confirm_phase_i_go.md (21635 bytes). Three sub-phases: (I.1) run all 12 spec acceptance gates in safe order -- read-only gates 1-11 first to capture clean evidence (Gate 5 is the FIRST end-to-end exercise of LAN bind + UFW + pg_hba scram-sha-256 + admin SCRAM password from a non-Postgres-internal context; Gate 11 uses LIKE-pattern smoke to avoid Bug B variable pollution), then Gate 12 restart-safety LAST since it's the only service-affecting gate (restart Beast container, verify subscription returns to all-r within 60s, slot remains active=t throughout, RestartCount expected to increment to 1 from this gate); (I.2) cleanup 11 intermediate files -- 9 on CiscoKid (B2b_phase_e/f/h logs, 3x controlplane_schema*.sql variants, pg_dump_stderr.log, phase_e/h_launch.sh) + 2 on Beast (controlplane_schema_filtered.sql + filtered_v2.sql); preserve /tmp/compose.yaml.b2b-pre-backup + /tmp/pg_hba.conf.original (Phase A rollback artifacts, kept through B2b CLOSED, future archive to .b2b-rollback-artifacts/ as P5 carryover); (I.3) ship report at /home/jes/control-plane/postgres/B2b_ship_report.md per spec lines 300-308: 12-gate scorecard with evidence, 16s CiscoKid downtime measurement (Phase E), slot LSN progression table (4 stages captured + Gate 12 post-restart), full 13-row table parity, 15s initial sync time, 4 spec deviations (scram-sha-256 amendment, ufw insert correction, Phase F failure-and-Option-B retry, Phase H verifier-bug Option A recovery), final config md5sums (pg_hba 2138efc3..., compose ffbfbfa8...), 6 P6 lessons, open carryovers (DOCKER-USER chain hardening, replicator-role separation, archive task). After PD review confirms all 12 gates PASS + cleanup + ship report, Paco runs independent verification gate from fresh shell, writes paco_response_b2b_independent_gate_pass_close.md, commits CHECKLIST flip `[~]` -> `[x]`. **B2b CLOSED.** P3 dependency unblock: Atlas-on-Beast charter implementation becomes executable; B2b's continuous publisher-side data feed is Atlas's hard prerequisite which is now satisfied.
- [x] **2026-04-26 Day 72** -- **B2b CLOSED.** Paco independent verification gate PASSED 12/12 from a fresh shell context: Gate 1 wal_level=logical PASS; Gate 2 LAN listener exactly 192.168.1.10:5432 with 0 localhost + 0 wildcard PASS (anchored regex per PD's Phase I correction); Gate 3 UFW [18] ALLOW IN from 192.168.1.152 before [19] DENY (v4) + [26] DENY (v6) PASS; Gate 4 hba_file=/etc/postgresql/pg_hba.conf with container md5 = host md5 = `2138efc3a90ab513cf5aa1fff1af613e` (bind mount integrity confirmed) PASS; Gate 5 PGPASSWORD=adminpass psql from Beast to 192.168.1.10:5432 returns 1 (LAN bind + UFW + scram-sha-256 + admin SCRAM all exercised) PASS; Gate 6 Beast \dn shows public + agent_os, mercury count=0 PASS; Gate 7 controlplane_pub schema-scoped 1 row all 4 DML flags + 13 tables in pg_publication_tables PASS; Gate 8 controlplane_sub enabled with subpublications={controlplane_pub}, not-r=0, total=13 PASS; Gate 9 slot active=t with restart_lsn=0/F230B30 confirmed_flush_lsn=0/F230B68 lag=0 bytes PASS; Gate 10 row count parity all 13 tables MATCH byte-identical (agent_tasks 46/46, messages 3/3, memory 4756/4756, chat_history 482/482, documents 203/203, tasks 83/83, plus all others MATCH) **5,795 total rows replicated** PASS; Gate 11 independent Paco smoke test (LIKE-pattern, unique title `B2b Paco close-out smoke`) -- INSERT replicated to Beast within 10s with byte-identical row id=`d845c258-c498-4b58-b9eb-c1fac633d1f2`, DELETE replicated within 10s both sides count=0 PASS; Gate 12 transitive verification of post-PD-restart steady state -- Beast Started=2026-04-27T00:13:57.800Z (post-Gate-12 restart), new WAL sender PID 6132 streaming async, slot persisted across restart with active=t throughout, LSN advancing in real-time (post-Paco-smoke 0/F23D678/0/F23D738 lag 0 bytes), CiscoKid Up healthy RestartCount=0 (since Phase E recreate) + Beast Up healthy RestartCount=0 (Docker semantics: RestartCount counts crash-driven restarts only, not external `docker compose restart`) PASS. Phase A rollback artifacts preserved on disk (/tmp/compose.yaml.b2b-pre-backup 506 bytes md5 b7bbe49c..., /tmp/pg_hba.conf.original 5743 bytes md5 3f1a04eb...). Ship report shipped at /home/jes/control-plane/postgres/B2b_ship_report.md (md5 58aa5477..., 17396 bytes / 230 lines, 12-gate scorecard + 16s downtime + 6-stage slot LSN progression + full 13-row parity table + 4 deviations + 6 P6 lessons + open carryovers). 19 docs in /docs/ across the B2b chain (7 paco_review + 2 paco_request + 9 paco_response + 1 ratification) + this close-out doc at docs/paco_response_b2b_independent_gate_pass_close.md (12459 bytes). 4 spec deviations all explicitly Paco-authorized: (1) Phase A scram-sha-256 amendment (admin password storage format), (2) Phase D `ufw insert 18` correction (rule ordering against pre-existing DENY), (3) Phase F failure recovery Option B (CREATE SCHEMA public collision; targeted rollback preserved B2a vector invariant), (4) Phase H verifier-bug recovery Option A (PG 16 char-strictness + psql -tA command-tag pollution; replication infrastructure was empirically healthy throughout). 6 P6 lessons banked (3 from Phase F failure + 3 from Phase H verifier-script-bug recovery). 9-phase span over ~3h elapsed Phase A through Paco close-out. **Logical replication CiscoKid -> Beast is LIVE, byte-perfect, real-time.** CHECKLIST line 29 flipped `[~]` -> `[x]` this commit. **P3 Atlas-on-Beast charter implementation now unblocked** -- B2b's continuous publisher-side data feed is Atlas's hard prerequisite, now satisfied; Beast is canonical Atlas home per CHARTERS_v0.1 + CAPACITY_v1.1 ratifications; Atlas build spec drafting can begin when CEO schedules it. P5 carryovers added during B2b: DOCKER-USER chain hardening for LAN-published Postgres, replicator-role separation (currently using shared admin/adminpass per Pick 3), archive of Phase A rollback artifacts to .b2b-rollback-artifacts/ subdir.
- [x] **2026-04-26 Day 72** -- B1 spec drafted (RATIFIED at draft time per CEO single-word approval of all 6 picks). MinIO single-node single-drive on Beast at /home/jes/minio-beast/ with bind-mount data at /home/jes/minio-data/. 6 ratified picks: (1) Docker Compose deployment matching B2a/B2b idiom; (2) image pinned by sha256 digest + RELEASE date tag (P6 lesson from B2a); (3) LAN-bind 192.168.1.152:9000 (S3 API) + :9001 (console) -- same gate stack as B2b enables LAN-direct CiscoKid backup jobs without SSH-tunnel scaffolding; (4) 3 pre-created buckets at first-boot via mc one-shot container (atlas-state for Atlas v0.1+, backups for CiscoKid DR, artifacts for portfolio assets) -- avoids per-consumer-spec bundling cost; (5) root-credentials-only in /home/jes/minio-beast/.env (chmod 600, gitignored, generated at install time via openssl rand -base64 30) -- per-svc IAM deferred to P5 carryover (matches B2b admin/adminpass logic); (6) SNSD on single 4.4TB drive -- erasure coding without multiple physical drives is theater, MinIO supports in-place expansion if a second drive lands. 6 phases (A capture, B compose+env+dirs, C UFW allow with no DENY collision since 9000/9001 unbound on Beast pre-B1, D first-boot+healthcheck-poll+bucket-create, E CiscoKid->Beast LAN smoke PUT/GET/DELETE round-trip, F restart-safety+cleanup+ship-report). 8-gate acceptance scorecard: container Up healthy + LAN listener exact + UFW 2 new allow rules + health endpoint 200 + 3 buckets present from both Beast and CiscoKid + 1KB smoke round-trip + persistence across restart + 0 ERROR/FATAL in container logs. End-to-end LAN+UFW+auth gate stack first exercised at Phase E (mirrors B2b Gate 5). Time estimate: 30-55 min PD + ~15 min Paco independent verification. Pre-spec sanity verified on Beast: Docker 28.2.2, Compose v5.1.3 (B2a-validated), no MinIO image pulled, ports 9000/9001 free, B2a precedent dir at /home/jes/postgres-beast/ with 1 healthy PG container only. SSH from Beast to CiscoKid currently does NOT have a key (Permission denied publickey,password) -- noted as P5 carryover for admin convenience but NOT a B1 blocker (CiscoKid initiates outbound to MinIO via S3 protocol port 9000, not via reverse SSH). Spec at /home/jes/control-plane/tasks/B1_minio_beast.md (18011 bytes). Status: RATIFIED, ready for PD via Cowork. P3 sequence reminder: B1 ships first, then Atlas v0.1 spec drafts (~6-8 phases), then Atlas v0.1 demo recording for LinkedIn/portfolio.
- [x] **2026-04-26 Day 72** -- B1 Phase A CONFIRMED by Paco. PD captured 4 read-only pre-state files on Beast: /tmp/B1_phase_a_ports.txt (md5 64e73e7d, 11 bytes, content 'ports free'), /tmp/B1_phase_a_ufw.txt (md5 e42db94e, 1392 bytes, 14 numbered rules), /tmp/B1_phase_a_home_layout.txt (md5 7c454123, 2004 bytes, 38 lines), /tmp/B1_phase_a_disk.txt (md5 fd2ed809, 129 bytes, /dev/sda2 4.4T total / 144G used / 4.0T avail / 4% used). Independent Paco cross-check from fresh Beast shell: ALL 4 MD5s MATCH PD report byte-for-byte. Greenfield confirmed: ports 9000/9001 free at re-check, /home/jes/minio-beast/ ABSENT, /home/jes/minio-data/ ABSENT, no control-minio-beast container, no quay.io/minio/minio image pulled. B2b infrastructure verified untouched: control-postgres-beast Up 2 hours healthy on 127.0.0.1:5432 with RestartCount=0. UFW shows expected 14 rules (11 IoT DENY [1]-[11] + 22/tcp [12] + 11434/tcp [13] + 8800/tcp [14]); ports 9000/9001 unbound. **Phase B GO** authorized at docs/paco_response_b1_phase_a_confirm_phase_b_go.md (11775 bytes). Three steps: (B.1) mkdir minio-beast + minio-data with chmod 700 on data dir + ls/stat verify (note: dropped the spec's `init/` subdir because B1 has no init scripts unlike B2a's PG SQL init -- bucket creation is via mc one-shot in Phase D, not init); (B.2) generate root creds with REFINED password pipeline `openssl rand -base64 60 | tr -d '/+=' | head -c 40` (vs spec literal `-base64 30`) + length-check-before-write to guarantee 40-char password (avoids edge case where stripped chars cluster and head -c 40 produces short password) + write to .env chmod 600 jes:jes + echo password ONCE for CEO 1Password recording; (B.3) `docker pull quay.io/minio/minio:latest` + capture deterministic digest via `docker inspect ... --format '{{index .RepoDigests 0}}'` (canonical `quay.io/minio/minio@sha256:<hex>` form) + write compose.yaml via heredoc with substituted digest, LAN-bind 192.168.1.152:9000/9001, bind-mount /home/jes/minio-data:/data, healthcheck `mc ready local` + start_period 15s + `docker compose config` validation. Pre-flight gotcha banked: if MinIO image lacks bundled `mc` binary (historically not always present), `curl -f http://127.0.0.1:9000/minio/health/live || exit 1` is pre-authorized as fallback healthcheck during Phase D as micro-correction. Two micro-refinements (-base64 60 and digest-via-RepoDigests-index) explicitly authorized in directive. Phase B is non-service-affecting (creates new files+dirs only); B2a/B2b infrastructure must remain untouched. 6-item Phase B acceptance gate. Phase C (UFW allow rules, simple `ufw allow` since no DENY collision verified) gated behind separate Paco GO.
- [x] **2026-04-26 Day 72** -- B1 PIVOTED from MinIO to **Garage v2.1.0**. Discovery during Phase B image-pull verification: `quay.io/minio/minio:latest` returned a 7-month-old build (`RELEASE.2025-09-07T16-13-09Z`); investigation surfaced that MinIO Community Edition went source-only October 2025 and the upstream GitHub repo was archived 2026-02-14 (read-only, no further commits, no Docker image updates). Last available image carries CVE-2025-62506 (CVSS 8.1, privilege escalation via session policy bypass in service accounts/STS) -- not exploitable in our LAN-only single-tenant threat model but signal that no future patches will land. Five-candidate exhaustive comparison performed (per CEO mantra: end is the beginning, exhaust possibilities before deciding): (1) Garage v2.1.0 (Rust, AGPL-3.0, single-container, Deuxfleurs); (2) SeaweedFS 3.x (Go, Apache 2.0, multi-container master+volume+filer+s3-gateway); (3) RustFS 1.0-alpha (Apache 2.0, MinIO drop-in claim, alpha = not foundational); (4) MinIO frozen Sept 2025 (accept CVE, deteriorating substrate); (5) MinIO from-source (build pipeline burden each CVE). Garage selected for: actively maintained substrate (v2.x stable), single-container operational fit (matches B2a/B2b idiom), single-node well-supported (replication_factor=1 + lmdb metadata), AGPL-3.0 acceptable for our internal-tool threat model (same legal status MinIO had). SeaweedFS disqualified despite Apache 2.0 maturity because multi-container architecture is solving a billions-of-files-O(1)-seek problem we don't have. RustFS disqualified for alpha-stage in foundational infra. MinIO frozen + from-source disqualified for deteriorating substrate. Original B1 MinIO spec at tasks/B1_minio_beast.md is DEPRECATED (kept in git history; new canonical spec at tasks/B1_garage_beast.md, 29228 bytes). Six original Picks carried forward + adapted: (1) Docker Compose [unchanged]; (2) digest-pinned image [now `dxflrs/garage@sha256:...` instead of MinIO]; (3) LAN bind [now 192.168.1.152:3900 S3 API only; admin 3903 localhost; RPC 3901/web 3902/k2v 3904 container-internal -- single UFW rule needed instead of two]; (4) 3 pre-created buckets at first boot [unchanged: atlas-state, backups, artifacts -- via `garage bucket create` CLI inside container instead of mc one-shot]; (5) single S3 root access key for all 3 buckets in v1 [analog of MinIO root creds; per-bucket keys are P5 carryover]; (6) SNSD on /home/jes/garage-data/{meta,data} [Garage docs prefer meta on SSD; not blocking for v1 single-drive context]. New spec has 6 phases: Phase A (port-3900-3904-free delta + MinIO rollback inventory), Phase A2 (rm -rf minio-beast/+minio-data/, docker rmi quay.io/minio/minio), Phase B (mkdir tree + garage.toml with embedded secrets via openssl rand chmod 600 + compose.yaml with 2 published ports + dxflrs/garage:v2.1.0 digest pin), Phase C (1 UFW rule for 3900 LAN), Phase D (deferred-subshell bootstrap: container start + healthcheck poll + cluster layout assign+apply + root S3 key creation + 3 bucket creates + 3 bucket-allow grants -- writes credentials to /home/jes/garage-beast/.s3-creds chmod 600 with AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_DEFAULT_REGION + AWS_ENDPOINT_URL exports), Phase E (CiscoKid -> Beast LAN smoke via aws-cli docker container with byte-parity md5 verification on 1KB random PUT/GET round-trip + DELETE), Phase F (restart safety + cleanup + ship report). 8-gate acceptance scorecard covering: container healthy, LAN listener exact (3900 LAN + 3903 localhost only, no 0.0.0.0), UFW 1 new rule, garage status with healthy node + assigned layout, 3 buckets present, root key scoped to all 3 buckets, byte-parity smoke from CiscoKid, persistence across restart. Pre-flight gotcha banked at draft time: dxflrs/garage image is scratch-based with NO bundled wget/curl/nc -- healthcheck MUST use `/garage status` (the application binary at container path /garage), authorized in spec. Three NEW P6 lessons banked at draft time: (#7) validate upstream maintenance status before drafting infra specs -- pre-spec web check on Docker image distribution should be standard for foundational substrates; MinIO archive happened ~2 months before our draft and a single search at draft time would have caught it. (#8) pivot mid-spec is the right call when foundation is wrong; catching at Phase B (1 phase deep) cost ~40-60 min net vs catching post-ship which would have cost hours of unwind across Atlas + CiscoKid backups + portfolio asset integration; CEO's measure-twice-cut-once and end-is-the-beginning mantras held. (#9) healthcheck binary must exist in target image -- scratch-based images carry only the application binary, no userspace tools; future spec drafts confirm healthcheck binary exists in target image before authoring. P6 session count now 9 (3 from Phase F failure recovery + 3 from Phase H verifier-bug + 3 from B1 pivot). Time estimate: 50-80 min PD execution + ~15 min Paco independent verification. PD next step: read new spec at tasks/B1_garage_beast.md, execute Phase A delta + Phase A2 rollback, surface paco_review_b1_garage_phase_a_capture.md.
- [x] **2026-04-26 Day 72** -- B1-Garage Phase A delta + A2 (MinIO rollback) CONFIRMED by Paco. PD captured 2 new files plus inventoried the MinIO Phase B artifacts being rolled back: /tmp/B1_garage_phase_a_ports.txt (md5 bed11886, content 'all garage ports free'), /tmp/B1_garage_phase_a_minio_inventory.txt (md5 3abe0f2e, records pre-A2 inventory of /home/jes/minio-beast/.env + compose.yaml + minio-data/ + quay.io/minio/minio:latest image ID 69b2ec208575). Phase A2 executed: rm -rf /home/jes/minio-beast/ + rm -rf /home/jes/minio-data/ + docker rmi quay.io/minio/minio:latest (Untagged + Deleted 10 layer fs entries, ~175MB freed). Independent Paco cross-check from fresh Beast shell: ALL 6 capture file md5s match PD report byte-for-byte (4 original B1 phase_a captures + 2 new garage delta captures). Live re-checks: Garage ports 3900-3904 still free, /home/jes/minio-beast/ + minio-data/ ABSENT, quay.io/minio/minio image count = 0, /home/jes/garage-beast/ + garage-data/ ABSENT (Phase B will create). **B2b infrastructure bit-identical pre/post pivot**: control-postgres-beast StartedAt = 2026-04-27T00:13:57.800746541Z (NANOSECOND-IDENTICAL match to PD report), RestartCount=0, Status=running, Health=healthy, port 5432 listener still 127.0.0.1 (B2a localhost bind). UFW count = 14 rules unchanged. The nanosecond-identical StartedAt is the strongest signal that the rollback was surgical and no Postgres subscriber state was disturbed during pivot. MinIO root password (L3I8HNZlh7NOXEXAhuBsrCTfmBOnqH0gLTuAvfh3) acknowledged unrecoverable -- correct, nothing depends on it, CEO can drop from 1Password. **Phase B GO** authorized at docs/paco_response_b1_garage_phase_a_confirm_phase_b_go.md (13348 bytes). Three steps: (B.1) mkdir garage-beast/ + garage-data/{meta,data} with chmod 700 on all data dirs + ls/stat verify; (B.2) generate 3 secrets via openssl rand (RPC_SECRET=64 hex, ADMIN_TOKEN=44 base64, METRICS_TOKEN=44 base64) + length-check-before-write to RPC_SECRET specifically + write to /home/jes/garage-beast/garage.toml chmod 600 jes:jes via heredoc with 28 lines (replication_factor=1, db_engine=lmdb, all 5 sub-sections [s3_api]+[s3_web]+[k2v_api]+[admin]+top-level RPC) [NOTE: directive said 26 lines, file is actually 28; off-by-2 directive count error, content matches spec verbatim]; (B.3) `docker pull dxflrs/garage:v2.1.0` + capture deterministic digest via {{index .RepoDigests 0}} format string + scratch-image confirmation via `docker run --entrypoint=/bin/sh ... -c 'which wget curl nc'` (expected: shell not present) and `docker run --entrypoint=/garage ... --version` (expected: prints version) + write compose.yaml with substituted digest + 2 published ports (192.168.1.152:3900 LAN S3 + 127.0.0.1:3903 localhost admin; RPC 3901 + web 3902 + k2v 3904 NOT published) + 3 bind mounts (garage.toml ro + meta + data) + scratch-aware healthcheck `[CMD, /garage, status]` + `docker compose config` validation. Anti-secret-leak note: Garage secrets live in garage.toml (bind-mounted file), NOT env_file -- so `docker compose config` does NOT print secrets (different from MinIO's env_file pattern that leaked password to terminal output). Two micro-refinements explicitly authorized in directive: (1) <REDACTED-IN-REVIEW-OUTPUT> discipline for secret values in PD's review doc to avoid chat-transcript leakage (learned from MinIO Phase B compose-config-prints-password incident); (2) explicit scratch-image confirmation step in B.3 (docker run with --entrypoint=/bin/sh expected to fail). Phase B is non-service-affecting; B2a/B2b infrastructure must remain bit-identical. 7-item Phase B acceptance gate. The 2 prior MinIO branch review docs (paco_review_b1_phase_a_capture.md + paco_review_b1_phase_b_compose.md) remain in /docs/ as the rolled-back-prior-attempt audit trail.
- [x] **2026-04-26 Day 72** -- B1-Garage Phase B (dirs + garage.toml + compose.yaml digest-pinned) CONFIRMED by Paco. PD executed 3 sub-steps clean: (B.1) mkdir tree -- /home/jes/garage-beast/ jes:jes 0775 (default umask, config dir non-secret), /home/jes/garage-data/ + meta/ + data/ all chmod 700 jes:jes (data tree restricted); (B.2) garage.toml generated with 3 openssl-rand secrets baked in (rpc_secret=64 hex, admin_token=44 base64, metrics_token=44 base64) -- chmod 600 jes:jes, 28 lines, 679 bytes, md5 `4837f4a845b3a126904f546059f97729`; secret values intentionally NOT echoed in any subshell output or review per <REDACTED-IN-REVIEW-OUTPUT> directive (learned from MinIO env_file leak); 3 secret lines confirmed present (1 each); (B.3) `docker pull dxflrs/garage:v2.1.0` (single layer pulled, ~25.3 MiB compressed -- 7x smaller than MinIO's 175 MiB / 10 layers, scratch-Rust economy showing); RepoDigest captured as `dxflrs/garage@sha256:4c9b34c113e61358466e83fd6e7d66e6d18657ede14b776eb78a93ee8da7cf6a`; scratch-image confirmation step PASSED (`/bin/sh: stat /bin/sh: no such file or directory: unknown` -- shell absent as predicted; `garage v2.1.0 [features: k2v, lmdb, sqlite, consul-discovery, kubernetes-discovery, metrics, telemetry-otlp, bundled-libs]` -- /garage binary present and lmdb feature confirmed matching db_engine config); compose.yaml written with substituted digest + 2 published ports (LAN 192.168.1.152:3900 + localhost 127.0.0.1:3903; RPC 3901/web 3902/k2v 3904 NOT published) + 3 bind mounts (garage.toml ro + meta + data) + scratch-aware healthcheck `[CMD, /garage, status]` -- chmod 664 jes:jes, 18 lines, 586 bytes, md5 `5f9a8878f65922fac5a56ee561f96883`; `docker compose config` exit 0 with full resolution. Independent Paco cross-check from fresh Beast shell: ALL 7 acceptance gates PASS byte-for-byte against PD report (dir perms / garage.toml metadata + secret-line counts / image digest / compose.yaml metadata / docker compose config resolution / scratch-image live re-test / B2b bit-identical). **B2b control-postgres-beast bit-identical across all of B1**: StartedAt = 2026-04-27T00:13:57.800746541Z (NANOSECOND match across Phase A / A2 / B), RestartCount=0, Health=healthy, port 5432 listener unchanged. UFW = 14 rules unchanged. Garage ports 3900-3904 still unbound (no container started yet). Two informational acknowledgments: (1) directive said 26 lines for garage.toml, actual is 28 -- directive count error on Paco side, content matches spec heredoc verbatim, banked as process note (structural-match check via section presence + secret-line counts is the durable validator, raw line count is weak); (2) image age ~7 weeks (Created 2025-09-15) -- v2.1.0 is current Garage upstream stable, NOT a sign of abandoned development like MinIO's 7-month gap. **Phase C GO** authorized at docs/paco_response_b1_garage_phase_b_confirm_phase_c_go.md (8670 bytes). One UFW allow rule: `sudo ufw allow from 192.168.1.0/24 to any port 3900 proto tcp comment 'B1: Garage S3 API'`. Admin port 3903 needs NO UFW rule -- bound to 127.0.0.1 via Docker port mapping, never reachable from LAN. No DENY collision (UFW IoT denies are individual host IPs not in 192.168.1.0/24 ALLOW range), so simple `ufw allow` lands at position [15], no insert needed (differs from B2b's 5432 case requiring `ufw insert 18`). 5-item Phase C acceptance gate including B2b bit-identical preservation. Phase D preview (deferred-subshell bootstrap: container start + healthcheck poll + cluster layout assign+apply + root S3 key creation + 3 bucket creates + 3 bucket-allow grants + .s3-creds write) gated behind separate Paco GO post-Phase-C.
- [x] **2026-04-26 Day 72** -- B1-Garage Phase C (UFW allow for port 3900 LAN) CONFIRMED by Paco. PD executed single command: `sudo ufw allow from 192.168.1.0/24 to any port 3900 proto tcp comment 'B1: Garage S3 API'`. New rule lands at position [15], cleanly behind existing [14] 8800/tcp ALLOW. Total UFW count: 15 (was 14, +1). Pre-existing 14 rules ([1]-[11] IoT DENY block, [12] 22/tcp SSH, [13] 11434/tcp Ollama, [14] 8800/tcp) all intact at original positions. /tmp/B1_garage_phase_c_ufw_post.txt written, 1483 bytes / 20 lines, md5 `934f41fdaa3aba85709e2798ee7c2805`. Independent Paco cross-check from fresh Beast shell: ALL 5 acceptance gates PASS byte-for-byte against PD report (rule placement + comment / total count / pre-existing rules intact / capture file md5 / no service-affecting changes). **B2b control-postgres-beast bit-identical across all 4 B1 phases now**: StartedAt = 2026-04-27T00:13:57.800746541Z (NANOSECOND-IDENTICAL across Phase A / A2 / B / C), RestartCount=0, Health=healthy. UFW rule alone does not bind any port -- Garage ports 3900-3904 still all-garage-ports-still-free; rule is dormant until Phase D starts the container. Validated directive's prediction that simple `ufw allow` lands at end without DENY collision (differs from B2b's 5432 case which required `ufw insert 18`). **Phase D GO** authorized at docs/paco_response_b1_garage_phase_c_confirm_phase_d_go.md (14477 bytes). Phase D is the most procedurally complex phase: deferred-subshell bootstrap pattern (B2b/D2 precedent) writing to /tmp/B1_garage_phase_d_bootstrap.log, completing 5 sub-steps async: (D.1) `docker compose up -d` first-boot of control-garage-beast container; (D.2) healthcheck poll cap 120s (24 polls x 5s; expected healthy in 30-45s including Docker's 3-check + start_period buffer); (D.3) capture node ID via `garage status` awk extraction + `garage layout assign -z dc1 -c 4T <node-id>` + `garage layout apply --version 1`; (D.4) `garage key create root` -> awk-parse Key ID + Secret key -> write /home/jes/garage-beast/.s3-creds chmod 600 jes:jes with 4 export lines (AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_DEFAULT_REGION=garage + AWS_ENDPOINT_URL=http://192.168.1.152:3900); (D.5) for atlas-state, backups, artifacts: `garage bucket create $B` + `garage bucket allow --read --write --owner $B --key root`; final `garage bucket list` + `garage key info root` for state confirmation. Garage 2.x CLI syntax locked in directive: `key create <n>` (NOT `key new --name`), `bucket allow --read --write --owner <bucket-name> --key <key-name>`, `layout apply --version 1` (greenfield first version), node ID via 16-char short prefix from `garage status` first column. Total Phase D wallclock budget ~3 min (~30-45s container boot, ~30s layout, ~10s key, ~10s buckets, ~30s state inspection). Secret-redaction discipline carries over: bootstrap log on Beast disk WILL contain literal ACCESS_KEY_ID + SECRET_KEY values (acceptable transitional state, not transmitted off-Beast); Phase F cleanup will archive log to /home/jes/garage-beast/.ship-report-inputs/ alongside .s3-creds; PD's Phase D review doc REDACTS both values per standing convention. 7-item Phase D acceptance gate: container Up healthy + RestartCount=0 / layout assigned (zone=dc1, capacity=4T, NOT 'NO ROLE ASSIGNED') / 3 buckets present / root key scoped to all 3 buckets with R+W+O each / .s3-creds chmod 600 jes:jes 6 lines / LAN listener exact (192.168.1.152:3900 + 127.0.0.1:3903 published; 3901+3902+3904 container-internal only) / B2b bit-identical preservation. Phase E preview (informational): first end-to-end exercise of LAN-bind + UFW + S3-key auth from CiscoKid via amazon/aws-cli docker container with byte-parity 1KB PUT/GET round-trip + DELETE -- this is the gate that proves the entire stack works before B1 close.
- [x] **2026-04-26 Day 72** -- B1-Garage Phase D (deferred-subshell bootstrap) CONFIRMED by Paco. PD executed full bootstrap end-to-end in **14 seconds** wallclock via deferred-subshell pattern (B2b/D2 precedent, 4th use): /tmp/phase_d_launch.sh fires `nohup bash -c '...' & disown` with internal `exec > /tmp/B1_garage_phase_d_bootstrap.log 2>&1`; subshell PID 83110 completed in 14s; main shell polled for `=== PHASE D COMPLETE ===` marker. (D.1) `docker compose up -d` brought up control-garage-beast in 1 second. (D.2) Healthcheck poll: starting -> starting -> healthy over 3 polls; **time-to-healthy = 11 seconds** against scratch-based image. (D.3) Cluster layout: NODE_ID `b90a0fe8e46f883c` captured 16-char prefix; `garage layout assign -z dc1 -c 4T` staged; `garage layout apply --version 1` allocated 256 partitions (~15.6 GB optimal partition size, 4.0 TB / 4.0 TB cluster capacity, replication factor 1, 100% utilization since single-node); post-status shows zone=dc1 capacity 4.0 TB DataAvail 4.4 TB (91.7%) v2.1.0. (D.4) `garage key create root`: KEY_ID 26 chars (GK-prefixed), SECRET 64 hex chars; both parsed via grep|sed (avoiding awk single-quote escape hell PD specifically called out); .s3-creds written to /home/jes/garage-beast/.s3-creds chmod 600 jes:jes 4 lines md5 `393cf89b0662d82588bb4136d0dee2e9` with AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_DEFAULT_REGION=garage + AWS_ENDPOINT_URL=http://192.168.1.152:3900. Variables `unset` from generation shell post-write. (D.5) 3 bucket creates (atlas-state d6fbcbd7f2def96f..., backups e37a914b6cc9cdd1..., artifacts 3f65a1fa52a7fc61...) + 3 `bucket allow --read --write --owner $B --key root` grants; final `garage bucket list` shows all 3 with global aliases; `garage key info root` shows BUCKETS FOR THIS KEY: 3 entries with RWO permissions on each. Independent Paco cross-check from fresh Beast shell: ALL 7 acceptance gates PASS byte-for-byte against PD report. **B2b control-postgres-beast bit-identical NANOSECOND-IDENTICAL across all 5 B1 phases now**: StartedAt = 2026-04-27T00:13:57.800746541Z (Phase A / A2 / B / C / D), RestartCount=0, Health=healthy. UFW unchanged at 15 rules. Secret-redaction discipline honored throughout: bootstrap log on Beast contains literal KEY_ID + SECRET values (acceptable transitional state, on-Beast disk only -- will be SHREDDED in Phase F per directive update); PD review doc REDACTED both values via `<REDACTED-IN-REVIEW-OUTPUT>`; .s3-creds chmod 600 jes:jes is the canonical on-disk creds store. Garage native UX feature: `key info` natively redacts Secret key as `(redacted)` in output. **Phase E GO** authorized at docs/paco_response_b1_garage_phase_d_confirm_phase_e_go.md (13829 bytes). Phase E is the FIRST end-to-end exercise of LAN-bind + UFW + S3 key auth from non-Beast context (mirrors B2b Gate 5 pattern). 9 sub-steps from CiscoKid via amazon/aws-cli docker container with byte-parity 1KB PUT/GET round-trip + DELETE.
- [x] **2026-04-27 Day 72/73 boundary** -- B1-Garage Phase E (CiscoKid -> Beast LAN smoke) CONFIRMED by Paco. PD executed 9 sub-steps clean: (E.0) `docker pull amazon/aws-cli:latest` -- digest sha256 bcc201d9..., 130 MiB; (E.1) `scp jes@192.168.1.152:/home/jes/garage-beast/.s3-creds /tmp/B1_garage_creds_ciscokid.env` chmod 600, md5 393cf89b0662d82588bb4136d0dee2e9 byte-identical to Beast (transfer integrity confirmed); (E.2) sourced creds via `set -a; . ; set +a`, length checks (KEY_ID 26 / SECRET 64 / region 'garage' / endpoint http://192.168.1.152:3900), `aws s3 ls` returned 3 buckets with creation timestamps 2026-04-27 04:16:29 matching Phase D bootstrap window -- **gate stack confirmed live (LAN-bind + UFW [15] + S3 SigV4 auth)**; (E.3) dd 1KB random payload, MD5_LOCAL = d19d6d0540d5d66aa8d29c9a15256af3; (E.4) PUT to s3://backups/B1_smoke.bin (Completed 1.0 KiB/1.0 KiB at 87.4 KiB/s); (E.5) LIST shows the object size 1024; (E.6) GET roundtrip /tmp/B1_smoke_roundtrip.bin (root-owned due to docker --network host bind-mount UID inheritance), MD5_ROUNDTRIP byte-identical = d19d6d0540d5d66aa8d29c9a15256af3, **BYTE_PARITY_OK** confirmed; (E.7) DELETE + post-DELETE LIST empty; (E.8) Beast `docker logs control-garage-beast | grep -iE 'error|fatal' | grep -v 'GET /' | wc -l` returned count=1, attribution clean -- match was `INFO garage_rpc::layout::manager: No valid previous cluster layout stored (IO error: No such file or directory ...)` -- expected first-boot INFO message before Phase D's `layout apply` (substring 'error' inside parenthetical of normal startup info, NOT operational error); (E.9) shred -u CiscoKid /tmp temp files (creds + smoke jes-owned shredded fine; roundtrip root-owned needed sudo shred recovery). All 3 files removed (3 No-such-file errors verified). Variables unset from shell post-cleanup. Independent Paco cross-check from fresh Beast + CiscoKid shells: ALL 7 acceptance gates PASS byte-for-byte against PD report. Beast: container Up About an hour healthy, 3 buckets persist with same IDs, backups bucket Objects=0 confirming DELETE worked, log error count=1 with attribution clean, UFW=15 rules. CiscoKid: 3 temp files shred-gone (3 No-such-file errors), amazon/aws-cli image cached (~625MB, harmless tooling persistence). **B2b control-postgres-beast bit-identical NANOSECOND-IDENTICAL across all 6 B1 phases now**: StartedAt = 2026-04-27T00:13:57.800746541Z (Phase A / A2 / B / C / D / E), RestartCount=0, Health=healthy, Up 5+ hours. The 6-phase StartedAt invariant is the strongest possible signal that B1 work has been entirely surgical. **P6 lesson #10 banked at this turn**: Docker `--network host -v <host>:<container>` writes new files into bind-mounted host path as the **container's UID** (root by default for amazon/aws-cli); cleanup needs sudo shred or chown first. P6 lessons banked Day 72 now total 10 (3 from B2b Phase F failure recovery + 3 from B2b Phase H verifier-bug + 3 from B1 pivot at draft time + 1 from Phase E docker volume permissions). **Phase F GO** authorized at docs/paco_response_b1_garage_phase_e_confirm_phase_f_go.md (17778 bytes). Phase F is the final phase before B1 close: (F.1) restart safety -- `docker compose restart` + healthcheck poll cap 120s + verify layout + 3 buckets + key permissions persist; expected POST_RESTART_STARTED to be new timestamp but state preserved; (F.2) cleanup with explicit secret discipline -- SHRED `/tmp/B1_garage_phase_d_bootstrap.log` (contains plaintext KEY_ID + SECRET) and `/tmp/phase_d_launch.sh` helper script; ARCHIVE non-secret Phase A/B/C captures + compose-config logs to `/home/jes/garage-beast/.ship-report-inputs/` chmod 700 jes:jes with files chmod 600; (F.3) ship report at `/home/jes/garage-beast/B1_ship_report.md` mode 644 with 17 required sections including pivot rationale + 6 Picks adapted + 8 spec gates with evidence + bind/UFW/health endpoint + image digest + file md5s + bucket inventory + secrets disclosure pattern + restart safety result + smoke byte-parity + B2b bit-identical confirmation + zero structural deviations + 5 P5 carryovers + 4 P6 lessons (#7-10) + time elapsed + open follow-ups. 7-item Phase F acceptance gate: restart safety / bootstrap log shredded / helper scripts shredded / non-secret captures archived chmod 600 in chmod 700 dir / ship report exists with 17 sections mode 644 / B2b unchanged / final state file md5s preserved post-restart. Phase F -> Paco independent close gate: re-verify ALL 8 spec gates from fresh shell (B2b discipline); if PASS, write paco_response_b1_garage_independent_gate_pass_close.md, move CHECKLIST B1 [~]->[x], banner audit entry B1 CLOSED. **Atlas v0.1 spec drafting unblocks at B1 close.**
- [x] **2026-04-26 Day 72** -- B1-Garage Phase D (deferred-subshell bootstrap) CONFIRMED by Paco. PD executed 5 sub-steps in 14 seconds via `/tmp/phase_d_launch.sh` heredoc-via-quoted-terminator pattern (subshell PID 83110 -> log /tmp/B1_garage_phase_d_bootstrap.log 9978 bytes). (D.1) `docker compose up -d` first-boot of control-garage-beast: network garage-beast_default created, container created+started in 1 second; up -d exit 0. (D.2) Healthcheck poll: starting -> starting -> healthy in 3 polls; **time-to-healthy 11 seconds** (well under expected 30-45s; scratch-aware /garage status check transitioned cleanly). (D.3) NODE_ID captured via `garage status` awk extraction: `b90a0fe8e46f883c` (16-char hex prefix, length-validated); `garage layout assign -z dc1 -c 4T b90a0fe8e46f883c` staged role; `garage layout apply --version 1` applied with output showing 'Optimal partition size: 15.6 GB / Usable capacity 4.0 TB (100.0%) / 256 partitions / replication factor 1'. Post-layout status shows node transitioned from 'NO ROLE ASSIGNED' to 'dc1 / 4.0 TB / 4.4 TB (91.7%) / v2.1.0'. (D.4) `garage key create root` -> output included Key ID (26 chars, GK<24hex> format) + Secret key (64 hex chars) + Created timestamp; both values parsed via grep|sed pipeline (avoiding awk single-quote escape complexity in bash heredoc); written to /home/jes/garage-beast/.s3-creds chmod 600 jes:jes with 4 export lines (AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_DEFAULT_REGION=garage + AWS_ENDPOINT_URL=http://192.168.1.152:3900); md5 `393cf89b0662d82588bb4136d0dee2e9`; vars `unset` from generation shell after write. (D.5) For atlas-state, backups, artifacts: `garage bucket create $B` returned bucket IDs (d6fbcbd7..., e37a914b..., 3f65a1fa... respectively); `garage bucket allow --read --write --owner $B --key root` granted RWO on each. Final `garage bucket list` shows all 3 with global aliases + creation 2026-04-27. Final `garage key info root` shows Validity=valid, Expiration=never, Can create buckets=false (correct: scoped key not super-admin), and 'BUCKETS FOR THIS KEY' enumerates all 3 with RWO each. Notable Garage UX: `key info` natively redacts Secret key as '(redacted)' (built-in defense). Independent Paco cross-check from fresh Beast shell: ALL 7 acceptance gates PASS byte-for-byte against PD report. Container StartedAt 2026-04-27T04:16:17.272248309Z, RestartCount=0, Status=running, Health=healthy. Layout state preserved. 3 buckets preserved. Root key scoped to all 3 with RWO. .s3-creds chmod 600 jes:jes / 4 lines / md5 393cf89b... / 4 export lines confirmed present (no values read by Paco -- structural verification only). LAN listener exact: ss -tln shows 127.0.0.1:5432 (B2a) + 127.0.0.1:3903 (Garage admin) + 192.168.1.152:3900 (Garage S3 LAN); RPC 3901 + web 3902 + k2v 3904 NOT published (container-internal only) -- defense-in-depth correct. **B2b control-postgres-beast bit-identical across all 5 B1 phases now**: StartedAt = 2026-04-27T00:13:57.800746541Z (NANOSECOND-IDENTICAL match across Phase A / A2 / B / C / D), RestartCount=0, Health=healthy. UFW = 15 rules unchanged. The 5-phase B2b StartedAt invariant is the strongest possible signal that B1 work has been surgical throughout. Secret-handling discipline honored: KEY_ID + SECRET in PD review doc shown only as `<REDACTED-IN-REVIEW-OUTPUT>`; live in .s3-creds (chmod 600 on Beast disk only) and bootstrap log (will be archived to /home/jes/garage-beast/.ship-report-inputs/ in Phase F). **Phase E GO** authorized at docs/paco_response_b1_garage_phase_d_confirm_phase_e_go.md (13829 bytes). Phase E is the FIRST end-to-end exercise of LAN-bind + UFW + S3 key auth from a non-Beast context (mirrors B2b Gate 5 pattern). 9 sub-steps from CiscoKid: pre-flight docker amazon/aws-cli pull, scp .s3-creds Beast->CiscoKid /tmp with md5-must-match-393cf89b verification, source creds + length checks (KEY_ID=26, SECRET=64), `aws s3 ls` confirms 3 buckets, dd 1KB random payload + capture MD5_LOCAL, PUT to s3://backups/, LIST confirms presence, GET roundtrip + MD5_ROUNDTRIP byte-parity check (must print BYTE_PARITY_OK), DELETE + verify empty, Beast-side log error count check (filter on error|fatal excluding GET/ normal-not-found), shred -u creds + smoke files. 7-item Phase E acceptance gate. Phase F preview (informational): docker compose restart persistence test + intermediate cleanup to .ship-report-inputs/ + ship report at /home/jes/garage-beast/B1_ship_report.md, then Paco independent gate -> close-out -> CHECKLIST [~] -> [x] -> **B1 CLOSED**, then Atlas v0.1 spec drafting (~6-8 phases, all dependencies satisfied B2b ✓ + B1 ✓).
---

## How to use this checklist

- **Every Paco session opens with a checklist read.** SESSION.md is history; CHECKLIST.md is current state.
- **Every closed task moves to the audit trail with a date.**
- **Every new commitment lands at the right priority tier with an explicit owner.**
- **No work happens that isn't on this list.** If it should happen, add it. If it shouldn't, don't.

